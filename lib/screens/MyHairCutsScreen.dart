import 'package:flutter/material.dart';

class MyHairCutScreen extends StatelessWidget {
  const MyHairCutScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('tela3'),
    );
  }
}
